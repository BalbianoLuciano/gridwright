---
description: Show gridwright runs and which stage each one is on
---

Run `gw status`, then `gw next` for the active run.

Report where things stand: the stage, who owns it, and whether it is waiting on
a human decision. If a stage failed, give the reason verbatim rather than
paraphrasing it into something friendlier.
